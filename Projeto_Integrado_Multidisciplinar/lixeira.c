/*
    printf("\nPor favor, digite o nome: ");
    fflush(stdin);
    gets(visitante.nome);

    printf("Por favor, digite a idade: ");
    fflush(stdin);
    gets(visitante.idade);

    printf("Por favor, digite o documento: ");
    fflush(stdin);
    gets(visitante.documento);

    fwrite(&visitante,sizeof(visitante),1,arquivo_cadastro);
    printf("\nLogin cadastrado com sucesso!\n");

    */
